# Node
node_modules
npm-debug.log

# PHP / Composer
vendor
# Keep composer.lock for reproducible builds in Docker (do not ignore it)

# Laravel
storage/*
!storage/app/public
bootstrap/cache/*

# Env and secrets
.env
.env.*

# Git and IDE
.git
.gitignore
.idea
.vscode

# Logs and temp
*.log
storage/logs/*
docker-compose.log
deploy.log

# OS
.DS_Store
Thumbs.db

# Build artifacts (we generate fresh React admin build inside the Docker image)
public/build

# Tests (not needed in production image)
tests/
.phpunit.cache/
.phpunit.result.cache

# Dev/deploy scripts (not needed in image)
sync-logs.sh
